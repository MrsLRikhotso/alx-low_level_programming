#include <stdio.h>
/**
  *main - Entry point
  *Description: 'write lowercase alphabets'
  *Return: always 0
  */
int main(void)
{
	int n;

	for (n = 0 ; n < 10 ; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
